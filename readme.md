descargar el proyecto e iniciar gitflow 
`git flow init`

hacer el pull en develop por si acaso

`git pull origin develop`

instalar las dependencias de angular y node

`npm install`

listo.

ramas develop y master
